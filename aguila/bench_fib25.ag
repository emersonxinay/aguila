funcion fib(n) {
    si n < 2 {
        retornar n
    }
    retornar fib(n - 1) + fib(n - 2)
}

inicio = reloj()
resultado = fib(25)
fin = reloj()

tiempo = fin - inicio
imprimir "fib(25) ="
imprimir resultado
imprimir "Tiempo:"
imprimir tiempo
