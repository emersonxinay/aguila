funcion fib(n) {
    si n < 2 {
        retornar n
    }
    retornar fib(n - 1) + fib(n - 2)
}

inicio = reloj()
resultado = fib(20)
fin = reloj()

tiempo = fin - inicio
imprimir "fib(20) ="
imprimir resultado
imprimir "Tiempo:"
imprimir tiempo
