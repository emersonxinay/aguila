#V2
funcion fib(n) { si n<2 { retornar n; } retornar fib(n-1)+fib(n-2); } imprimir(fib(5)); salir
funcion fib(n) { si n<2 { retornar n; } retornar fib(n-1)+fib(n-2); } imprimir(fib(8)); imprimir(fib(9)); salir
funcion fib(n) { si n<2 { retornar n; } retornar fib(n-1)+fib(n-2); } imprimir(fib(9)); salir
a=30
b=40
a+b
si a >=20 {\nimprimir("eres mayor ")\n} sino {\nimprimir("eres menor")\n}
a*8
a/5
a = 20.5
a
a=30.5
a*2
a=2.5
b=2.3
a+b
a*b
imprime(a*b)
imprime(a"{a*b}")
imprime(a"{a*b} vamos con todo")
