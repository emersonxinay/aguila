funcion fib(n) {
    si n <= 1 {
        retornar n
    }
    retornar fib(n - 1) + fib(n - 2)
}

inicio = reloj()
resultado = fib(40)
fin = reloj()

tiempo_segundos = fin - inicio
imprimir("Águila: Fib(40) =")
imprimir(resultado)
imprimir("Tiempo (segundos):")
imprimir(tiempo_segundos)
